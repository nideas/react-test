var React = require('react');


var App = React.createClass({
    render: function() {
        return (
            <div>Cloud9 React config and test before Heroku deploy.</div>
        );
    }
});

module.exports = App;